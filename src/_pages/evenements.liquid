---
layout: page
title: Plantations, chantiers participatifs et conférences autour de la forêt comestible
---

<div class="mx-auto max-w-7xl py-8 px-4 sm:py-12 sm:px-6 lg:flex lg:justify-between lg:px-8">
    <div class="max-w-7xl">
        <h2 class="text-4xl font-bold tracking-tight text-gray-900 sm:text-5xl lg:text-6xl">Événements</h2>
        <p class="mt-5 text-xl text-gray-500">Des chantiers participatifs, des plantations, des conférences et des projections autour de la forêt comestible</p>
    </div>
</div>

<div class="mx-auto max-w-7xl sm:py-2 sm:px-6 lg:px-8">
  <div class="space-y-12 sm:grid sm:grid-cols-1 sm:gap-x-6 sm:gap-y-12 sm:space-y-0 lg:grid-cols-2 lg:gap-x-8">
    {% for event in collections.events.resources %}
      <div class="max-w-sm bg-white border border-gray-200 rounded-lg shadow-md ease-in duration-300 hover:shadow-lg dark:bg-gray-800 dark:border-gray-700">
          <a href="{{ event.relative_url }}">
            <div class="relative">
              <img class="rounded-t-lg" src="{{ event.data.image | relative_url }}" alt="({{ event.data.title }})" />
              <div class="absolute bottom-2 right-2">
                <span class="inline-flex items-center rounded-full bg-yellow-100 px-3 py-0.5 text-sm font-medium text-yellow-800">{{ event.data.location }}</span>
              </div>
              <div class="absolute bottom-3 left-3">
              </div>
            </div>
          </a>
          <div class="p-5">
              <h3 class="mb-2 text-2xl font-bold tracking-tight text-gray-900 dark:text-white">
                <a href="{{ event.relative_url }}">{{ event.data.title }}</a>
              </h3>
              <p class="mb-3 font-normal text-gray-700 dark:text-gray-400">{{ event.data.summary }}</p>
              <p class="mb-3 font-bold text-gray-500">
                {{ event.data.event_date }}
              </p>
              <a href="{{ event.relative_url }}" class="inline-flex items-center px-3 py-2 text-sm font-medium text-center text-white bg-semisto-1 rounded-lg hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 dark:bg-blue-600 dark:hover:bg-blue-700 dark:focus:ring-blue-800">
                Infos et inscriptions
                <svg aria-hidden="true" class="w-4 h-4 ml-2 -mr-1" fill="currentColor" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path fill-rule="evenodd" d="M10.293 3.293a1 1 0 011.414 0l6 6a1 1 0 010 1.414l-6 6a1 1 0 01-1.414-1.414L14.586 11H3a1 1 0 110-2h11.586l-4.293-4.293a1 1 0 010-1.414z" clip-rule="evenodd"></path></svg>
              </a>
          </div>
      </div>
    {% endfor %}
  </div>
</div>